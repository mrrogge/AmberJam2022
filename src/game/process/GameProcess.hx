package game.process;

class GameProcess extends core.Process {
    public var coms = new ComStore();

    override function onInit() {
        super.onInit();
    }

    override function onUpdate(dt:Float) {
        super.onUpdate(dt);
    }

    override function onKilled() {
        super.onKilled();
    }

    function spawnDad() {

    }
}